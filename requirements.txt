# Core Dependencies (from MoneyPrinter)
numpy>=1.24.0
moviepy==1.0.3
ffmpeg-python>=0.2.0
Pillow>=9.0.0
requests>=2.28.0
toml>=0.10.0

# AI & LLM (from Viral-Faceless)
google-generativeai>=0.3.0
openai>=1.0.0

# TTS (Hybrid: Piper + Edge + Optional Paid)
# Piper: Run services/piper/ Docker container (no pip install needed)
edge-tts>=6.1.0

# Optional: Azure TTS (paid, last resort fallback)
# azure-cognitiveservices-speech>=1.30.0

# Optional: Google Cloud TTS (paid, last resort fallback)
# google-cloud-texttospeech>=2.14.0

# Subtitle Alignment (Aeneas) - OPTIONAL, install manually if needed
# aeneas>=1.7.3
# Install: pip install aeneas (requires numpy, ffmpeg, espeak)

# YouTube API (from YT-Automation-Agent)
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.4.0

# Batch Processing
tqdm>=4.65.0
pytz>=2023.0

# Web UI (Optional)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0